This directory owns automated verification for the whole repo.

- Every new public behavior needs at least one focused test.
- Prefer small fixtures and deterministic mock servers.
- Cover extreme cases: malformed config, invalid extra JSON, key failover, unsupported provider options, async polling failures, and temp URL warnings.


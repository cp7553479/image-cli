This module owns normalized request, response, alias, and validation logic.

- No filesystem or network calls here.
- Provider plugins consume normalized protocol types from this module.
- Add tests for every parsing, normalization, and reserved-key rule.

